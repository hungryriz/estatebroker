<?php

use Illuminate\Database\Seeder;

class DealDetailsTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
        //
  //       $numberOfDeals = 0;
  //       $listings = App\Model\Listing::all();
		// foreach($listings as $listing) {
		// 	DealDetail::create([
		// 		'listing_id' => $listing->id,
		// 		'deal_name' => 'Deal Name',
		//         'deal_time' => 'status',
		//         'created_at' => Carbon\Carbon::now()->format(),
		//         'updated_at' => Carbon\Carbon::now()->format(),
		// 	]);
		// }
    }
}
